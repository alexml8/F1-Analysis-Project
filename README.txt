ВАЖНО!!!

В файле F1_spending_data_parsing.py содержится парсинг с сайта f1budgetcup.com

В файле spending_data_correction.ipynb содержится корректировка данных, спарсенных с сайта

В файле kaggle_data.ipynb содержится корректировка данных, загруженных с каггла, которые хранятся в папке archive_kaggle

В папке data содержится вся база анализируемых данных

В файле ipynb_fixing.py написан код, который исправляет проблему чтения основного файла проекта, перезаписывая его в новый файл, который читается. 

В файле EDA_F1.ipynb содержится (но его никак на гитхабе не прочитать, поэтому я добавил файл EDA_F1_fixed.ipynb, который уже читается) основная часть проекта (EDA, графики, корреляционный анализ, машинное обучение и проверка гипотез)
